const branding = {
  appName: process.env.APP_NAME || 'Service CRM Admin',
  shortName: 'Service CRM',
  tagline: 'Lead intake, verification and approval operations',
  supportEmail: process.env.SUPPORT_EMAIL || 'support@example.com'
};

module.exports = { branding };
