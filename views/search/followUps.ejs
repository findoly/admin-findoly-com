<%- include('../partials/pageStart', { subtitle: 'Search follow-up tasks' }) %>
<%- include('../partials/searchTabs') %>

<div class="card">
  <div class="card-header d-sm-flex align-items-sm-center py-sm-0">
    <div>
      <h5 class="py-sm-2 my-sm-1 mb-0">Follow-up Search</h5>
      <div class="text-muted fs-sm">Search only follow-up tasks. Open the related enquiry from the View button.</div>
    </div>
    <div class="mt-2 mt-sm-0 ms-sm-auto"><span class="badge bg-primary rounded-pill"><%= pagination.total %> found</span></div>
  </div>
  <div class="card-body border-top">
    <form method="get" class="row g-3 align-items-end">
      <div class="col-xl-5 col-lg-6"><label class="form-label">Search text</label><div class="form-control-feedback form-control-feedback-start"><input class="form-control" name="q" placeholder="Title, owner, customer, enquiry ID..." value="<%= filters.q || filters.search || '' %>"><div class="form-control-feedback-icon"><i class="ph-magnifying-glass"></i></div></div></div>
      <div class="col-xl-2 col-lg-3 col-sm-6"><label class="form-label">Status</label><select class="form-select" name="status"><option value="">All statuses</option><% statuses.forEach((status)=>{ %><option value="<%= status %>" <%= filters.status === status ? 'selected' : '' %>><%= humanize(status) %></option><% }) %></select></div>
      <div class="col-xl-2 col-lg-3 col-sm-6"><label class="form-label">Enquiry ID</label><input class="form-control" name="enquiryId" placeholder="enq_..." value="<%= filters.enquiryId || '' %>"></div>
      <div class="col-xl-1 col-lg-3 col-sm-6"><label class="form-label">Per page</label><select class="form-select" name="pageSize"><% [10,25,50,100].forEach((size)=>{ %><option value="<%= size %>" <%= String(filters.pageSize || 25) === String(size) ? 'selected' : '' %>><%= size %></option><% }) %></select></div>
      <div class="col-xl-2 col-lg-3 col-sm-6"><button class="btn btn-primary w-100" type="submit"><i class="ph-magnifying-glass me-2"></i>Search</button></div>
    </form>
  </div>
  <div class="table-responsive">
    <table class="table text-nowrap">
      <thead><tr><th>Follow-up</th><th>Customer</th><th>Enquiry</th><th>Owner</th><th>Channel</th><th>Status</th><th>Due</th><th class="text-center" style="width:120px">Action</th></tr></thead>
      <tbody>
        <% records.forEach((item) => { %>
          <tr>
            <td><div class="fw-semibold"><%= item.title || item.id %></div><div class="text-muted fs-sm"><%= item.notes || '' %></div></td>
            <td><%= item.customerName || '-' %></td>
            <td><% if (item.enquiryId) { %><a href="/enquiries/<%= item.enquiryId %>"><%= item.enquiryId %></a><% } else { %>-<% } %></td>
            <td><%= item.owner || '-' %></td>
            <td><%= humanize(item.channel) %></td>
            <td><%- include('../partials/statusBadge', { value: item.status, humanize }) %></td>
            <td><%= formatDate(item.dueAt) %></td>
            <td class="text-center"><div class="d-inline-flex gap-1"><a href="/follow-ups/<%= item.id %>" class="btn btn-outline-primary btn-sm"><i class="ph-eye me-1"></i>View</a><a href="/follow-ups/<%= item.id %>/edit" class="btn btn-outline-secondary btn-sm"><i class="ph-pencil me-1"></i>Edit</a></div></td>
          </tr>
        <% }) %>
        <% if (!records.length) { %><tr><td colspan="8" class="text-center text-muted py-4">No follow-ups found.</td></tr><% } %>
      </tbody>
    </table>
  </div>
  <%- include('../partials/pagination', { pagination }) %>
</div>

<%- include('../partials/pageEnd') %>
